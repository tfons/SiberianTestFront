<div class="top-row pl-4 navbar navbar-dark">
    <a class="navbar-brand" href="">SiberianTestFront</a>
    <button class="navbar-toggler" @onclick="ToggleNavMenu">
        <span class="navbar-toggler-icon"></span>
    </button>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <ul class="nav flex-column">
        <li class="nav-item px-3">
            <NavLink class="nav-link" href="restaurantes" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Restaurantes
            </NavLink>
        </li>

        <li class="nav-item px-3">
            <NavLink class="nav-link" href="ciudades" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Ciudades
            </NavLink>
        </li>

        <li class="nav-item px-3">
            <NavLink class="nav-link" href="token" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Generar Token
            </NavLink>
        </li>
    </ul>
</div>

@code {
    private bool collapseNavMenu = true;

    private string NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
